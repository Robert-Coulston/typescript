import { Chicken } from "./chicken";

// dont forget to run the default build task which watch for changes in code and compile it to dist folder
console.log('Hello, world!');

const chicken = new Chicken();
chicken.cluck();